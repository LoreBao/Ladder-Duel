{
  "name": "ladder-duel",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm run build",
    "preview": "cd apps/web && npm run preview"
  }
}